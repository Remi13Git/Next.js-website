/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export', 
  basePath: '/Next.js-website',
  assetPrefix: '/Next.js-website/'
};

export default nextConfig;
